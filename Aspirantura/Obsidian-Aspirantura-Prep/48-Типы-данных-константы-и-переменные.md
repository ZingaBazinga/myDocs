---
title: "Вопрос 48 — Типы данных, способы задания. Константы и переменные"
tags:
    - aspirantura
    - exam-prep
    - programming
    - data-types
question: 48
block: "Системы и языки программирования"
status: in-progress
sources:
    - "Страуструп Б. — Язык программирования C++"
    - "Сузи Р.А. — Язык программирования Python"
    - "Вабищевич П.Н. — Численные методы. Вычислительный практикум (Python)"
---

# Вопрос 48. Типы данных, способы задания. Константы и переменные

← [[00-MOC-Aspirantura-Prep]] · Предыдущий: [[47-Системы-и-языки-программирования]] · Программа: [[Вопросы#^q48|Вопросы]]

## Краткий ответ (для устного экзамена)

> [!abstract] Простыми словами — вся суть
> **Тип данных** = множество значений + допустимые операции + способ представления в памяти.
> **Классификация типов:**
>
> - **простые (скалярные):** целые, вещественные, символьные, логические;
> - **составные:** массивы, строки, записи;
> - **абстрактные (АТД):** списки, классы и т.п.
>   **Способы задания:** явное объявление типа (C++, Java) или динамическая типизация (Python).
>   **Константа** — значение не изменяется в ходе выполнения программы.
>   **Переменная** — именованная область памяти: имя + тип + значение + адрес.
>   Главные атрибуты переменной: имя, тип, значение, адрес; у константы — только значение и тип.
>   В C++: `int a = 5; const double PI = 3.14159;`
>   В Python: `a = 5`, `PI = 3.14159` (тип определяется автоматически).

---

## Полный ответ

> [!abstract] Связный ответ комиссии
>
> **Определение.**
> Тип данных — формальная характеристика: (1) **домен** значений; (2) допустимые **операции**; (3) **представление** в памяти.
> Тип — «паспорт» значения, не путать с **переменной** (именованная область памяти этого типа).
> Для `int`: диапазон порядка $-2^{31}\ldots 2^{31}-1$, операции $+,-,*,/,\%$, двоичное дополнение, обычно 4 байта.
>
> **Классификация.**
>
> - **Простые (скалярные):** целый (`int`, `long`; Python `int`), вещественный (`float`, `double`; Python `float`), символьный (`char`; в Python одноэлементная `str`), логический (`bool`).
> - **Составные:** **массив** (`double a[10]`), **строка**, **запись/структура** (`struct Point { double x, y; }`), **кортеж**.
> - **АТД:** тип задан **операциями**, без фиксации представления («чёрный ящик»): список, стек, очередь, дерево, множество, **класс**.
>
> **Способы задания.**
> **Статическая** типизация (C++, Java): тип объявляется явно, проверка при компиляции (`int a = 5;`).
> **Динамическая** (Python): тип определяется при выполнении (`a = 5` → `int`). В Python типы **есть**.
> **Сильная** типизация не смешивает несовместимые типы без явного преобразования; **слабая** допускает неявные преобразования (скрытые ошибки).
>
> **Память.**
> Целые — двоичная система, отрицательные в **дополнительном коде**.
> Вещественные — IEEE 754: $x = (-1)^s \cdot m \cdot 2^{e}$ (`float` ~ 7 цифр, `double` ~ 15).
> Хранение приближённое: $0.1+0.2\neq 0.3$ — источник ошибок округления в ЧМ.
> Символы — ASCII/Unicode; логические: `True` = 1, `False` = 0.
>
> **Константа.**
> Значение **не изменяется** при выполнении.
> Виды: литералы, именованные, макросы `#define`, перечисления.
> C++: `const double PI = 3.14159;`, `constexpr`.
> Python: нет жёсткого `const` — соглашение ЗАГЛАВНЫМИ; это не то же, что `const` в C++.
>
> **Переменная.**
> Атрибуты: **имя**, **тип**, **значение**, **адрес**; значение может меняться.
> **Объявление** (`int a;`) ≠ **инициализация** (`int b = 10;`).
> **Область видимости** — где имя доступно; **время жизни** — когда память выделена.
> Локальные, глобальные, статические (сохраняют значение между вызовами).
>
> **Пример.** Разностная схема: `const int N = 100` — узлы; `h`, `tau` — шаги (переменные); `double u[N]` — массив.
> Константы — параметры и размеры сеток; переменные — шаги, решение, счётчики.
>
> **Комиссии.** Три компонента типа, три класса, статика/динамика, `const` vs соглашение Python, четыре атрибута переменной, IEEE 754.

---

## 1. Понятие типа данных

### Определение

**Формулировка (учебник):** тип данных — формальная характеристика данных, определяющая:

1. **множество допустимых значений** (домен);
2. **множество допустимых операций** над этими значениями;
3. **способ представления** в памяти компьютера.

### Три компонента типа

| Компонент        | Что определяет           | Пример для `int`                         |
| ---------------- | ------------------------ | ---------------------------------------- |
| Домен (значения) | какие значения допустимы | целые числа $-2^{31} \ldots 2^{31}-1$    |
| Операции         | что можно делать         | $+$, $-$, $*$, $/$ (целочисленное), $\%$ |
| Представление    | как хранится в памяти    | двоичное дополнение, 4 байта             |

> [!tip] Простыми словами
> Тип — это «паспорт данных»: говорит, что за значение, что с ним можно делать и сколько памяти оно занимает.

---

## 2. Классификация типов данных

### Общая схема

$$
\text{Типы данных}
\begin{cases}
\text{Простые (скалярные)} \\
\text{Составные (структурированные)} \\
\text{Абстрактные (АТД)}
\end{cases}
$$

### 2.1. Простые (скалярные) типы

| Тип          | Примеры значений | C++                    | Python                        |
| ------------ | ---------------- | ---------------------- | ----------------------------- |
| Целый        | $42$, $-7$       | `int`, `long`, `short` | `int`                         |
| Вещественный | $3.14$, $-0.5$   | `float`, `double`      | `float`                       |
| Символьный   | `'A'`, `'я'`     | `char`                 | `str` (одноэлементная строка) |
| Логический   | `True`, `False`  | `bool`                 | `bool`                        |

### 2.2. Составные (структурированные) типы

| Тип                | Описание                                 | Пример                          |
| ------------------ | ---------------------------------------- | ------------------------------- |
| Массив             | фиксированный набор однотипных элементов | `double a[10]`                  |
| Строка             | последовательность символов              | `std::string s`                 |
| Запись / структура | набор разнотипных полей                  | `struct Point { double x, y; }` |
| Кортеж             | неизменяемая последовательность          | `(1, 2, 3)` в Python            |

### 2.3. Абстрактные типы данных (АТД)

**Формулировка (учебник):** абстрактный тип данных — тип, определяемый через набор операций, без фиксации внутреннего представления.

Примеры: список, стек, очередь, дерево, множество, класс.

> [!tip] Простыми словами
> АТД — «чёрный ящик»: важно не как хранится, а какие операции доступны. Подробнее — [[50-Основы-объектно-ориентированного-программирования|Вопрос 50]] (ООП).

---

## 3. Способы задания типов

### Статическая и динамическая типизация

| Способ                     | Суть                                                     | Примеры языков       |
| -------------------------- | -------------------------------------------------------- | -------------------- |
| **Статическая** (явная)    | тип объявляется в коде и проверяется на этапе компиляции | C, C++, Java, Pascal |
| **Динамическая** (неявная) | тип определяется автоматически во время выполнения       | Python, JavaScript   |

### Примеры

**C++ (статическая типизация):**

```cpp
int a = 5;              // явно: целое
double x = 3.14;        // явно: вещественное
const double PI = 3.14159265;  // константа
```

**Python (динамическая типизация):**

```python
a = 5          # int определяется автоматически
x = 3.14       # float
PI = 3.14159265  # float (константа — по соглашению)
```

### Сильная и слабая типизация

| Характеристика | Суть                                                                     | Примеры       |
| -------------- | ------------------------------------------------------------------------ | ------------- |
| **Сильная**    | язык не позволяет смешивать несовместимые типы без явного преобразования | Python, Java  |
| **Слабая**     | неявные преобразования между типами допустимы                            | C, JavaScript |

> [!tip] Простыми словами
> Сильная типизация: «нельзя сложить число и строку без явного перевода». Слабая: «язык сам как-нибудь преобразует» (что может приводить к скрытым ошибкам).

---

## 4. Представление данных в памяти

### Целые числа

Хранятся в **двоичной системе**; отрицательные — в **дополнительном коде**.

| Тип (C++)   | Размер  | Диапазон                  |
| ----------- | ------- | ------------------------- |
| `int`       | 4 байта | $-2^{31} \ldots 2^{31}-1$ |
| `long long` | 8 байт  | $-2^{63} \ldots 2^{63}-1$ |

### Вещественные числа (IEEE 754)

**Формулировка (учебник):** число с плавающей точкой представляется в виде

$$
x = (-1)^s \cdot m \cdot 2^{\,e},
$$

где $s$ — знак, $m$ — мантисса, $e$ — порядок.

| Тип (C++) | Размер  | Точность                |
| --------- | ------- | ----------------------- |
| `float`   | 4 байта | $\sim 7$ значащих цифр  |
| `double`  | 8 байт  | $\sim 15$ значащих цифр |

> [!important] На экзамене
> Вещественные числа хранятся **приближённо**: $0.1 + 0.2 \ne 0.3$ в двоичной арифметике. Это источник ошибок округления в численных методах.

### Символы и логические

- Символы: коды таблиц (ASCII, Unicode) — целое число, отображённое на символ.
- Логические: `True` = 1, `False` = 0.

---

## 5. Константы

### Определение

**Формулировка (учебник):** константа — именованное или литеральное значение, **не изменяемое** в процессе выполнения программы.

### Виды констант

| Вид              | Пример (C++)                 | Пример (Python)                |
| ---------------- | ---------------------------- | ------------------------------ |
| **Литеральная**  | `42`, `3.14`, `'A'`          | `42`, `3.14`, `"hello"`        |
| **Именованная**  | `const double PI = 3.14159;` | `PI = 3.14159` (по соглашению) |
| **Макрос**       | `#define N 100`              | —                              |
| **Перечисление** | `enum Color { RED, GREEN };` | `from enum import Enum`        |

### Константы в C++

```cpp
const int MAX_SIZE = 1000;      // нельзя изменить
constexpr double PI = 3.14159;  // известно на этапе компиляции
```

### Константы в Python

В Python нет синтаксиса «настоящей» константы — используется **соглашение** (имя заглавными буквами):

```python
MAX_SIZE = 1000   # константа по соглашению
PI = 3.14159265
```

> [!tip] Простыми словами
> В C++ `const` — «запрет на изменение» enforced компилятором. В Python `PI = 3.14` — просто договорённость между программистами.

---

## 6. Переменные

### Определение

**Формулировка (учебник):** переменная — именованная область памяти, имеющая **имя**, **тип**, **значение** и **адрес**; значение может изменяться в процессе выполнения программы.

### Атрибуты переменной

| Атрибут                 | Что это                  | Пример       |
| ----------------------- | ------------------------ | ------------ |
| **Имя** (идентификатор) | как обращаемся           | `counter`    |
| **Тип**                 | какие значения допустимы | `int`        |
| **Значение**            | текущее содержимое       | `42`         |
| **Адрес**               | где лежит в памяти       | `0x7ffee...` |

### Объявление и инициализация

**C++:**

```cpp
int a;          // объявление (без инициализации)
int b = 10;     // объявление + инициализация
double x = 0.0;
```

**Python:**

```python
a = 10          # создаётся и инициализируется
x = 0.0
```

### Область видимости и время жизни

| Характеристика        | Суть                                             |
| --------------------- | ------------------------------------------------ |
| **Область видимости** | где в коде переменная доступна                   |
| **Время жизни**       | когда память под переменную выделена/освобождена |

Виды переменных по области видимости:

- **локальные** — внутри функции, живут пока функция работает;
- **глобальные** — видны во всём модуле;
- **статические** — сохраняют значение между вызовами.

> [!tip] Простыми словами
> Локальная переменная — «жилец квартиры»: появилась при вызове функции, исчезла при выходе. Глобальная — «житель дома»: существует всё время работы программы.

---

## 7. Сравнение: константа vs переменная

| Критерий             | Константа                                | Переменная                    |
| -------------------- | ---------------------------------------- | ----------------------------- |
| Изменение значения   | запрещено                                | разрешено                     |
| Ключевые слова (C++) | `const`, `constexpr`                     | —                             |
| Соглашение (Python)  | ЗАГЛАВНЫЕ_БУКВЫ                          | строчные_буквы                |
| Назначение           | фиксированные параметры (π, размерности) | изменяемые данные в алгоритме |

---

## 8. Примеры в контексте численных методов

### C++ (разностная схема из [[46-Разностные-методы-решения-нестационарных-задач-математической-физики|Вопроса 46]])

```cpp
const int N = 100;          // число узлов сетки — константа
const double a = 1.0;       // коэффициент теплопроводности
double h = 1.0 / N;         // шаг по пространству — переменная
double tau = 0.5 * h * h;   // шаг по времени — переменная
double u[N];                // массив — составной тип
```

### Python (аналог)

```python
N = 100          # константа по соглашению
a = 1.0
h = 1.0 / N
tau = 0.5 * h * h
u = [0.0] * N    # список (составной тип)
```

> [!tip] Простыми словами
> В численных расчётах константы — это физические параметры и размеры сеток, переменные — шаги, текущие значения решения, счётчики циклов.

---

## 9. Типичные ошибки на экзамене

> [!failure] Частые ошибки
> Путать **тип данных** (множество значений + операции + представление) и **переменную** (именованная область памяти определённого типа).
> Считать, что в Python нет типов — они есть, но определяются динамически.
> Утверждать, что `const` в C++ и заглавное имя в Python — одно и то же: в Python это только соглашение.
> Забывать, что вещественные числа хранятся приближённо (IEEE 754).
> Путать **объявление** (`int a;`) и **инициализацию** (`a = 5`).
> Не различать область видимости и время жизни переменной.
> Не приводить примеры из конкретных языков (C++, Python).

---

## 10. Связь с другими темами

> [!tip] Простыми словами
> [[47-Системы-и-языки-программирования|Вопрос 47]] — типы данных как базовая конструкция любого языка программирования.
> [[49-Выражения-операции-операторы|Вопрос 49]] — операции над данными определяются их типом.
> [[50-Основы-объектно-ориентированного-программирования|Вопрос 50]] — АТД и классы как развитие понятия типа.
> [[51-Прикладное-программное-обеспечение-математического-моделирования|Вопрос 51]] — библиотеки работают с типами данных (массивы NumPy и т.п.).
> [[46-Разностные-методы-решения-нестационарных-задач-математической-физики|Вопрос 46]] — сеточные функции как массивы переменных.

---

## 11. Контрольные вопросы

### 1. Тип данных

- [x] Что такое тип данных? Назовите три его компонента.

**Ответ.** Формальная характеристика данных: (1) **домен** — множество допустимых значений; (2) **операции** над ними; (3) **представление** в памяти. Для `int`: значения $-2^{31}\ldots 2^{31}-1$, операции $+,-,*,/,\%$, двоичное дополнение, 4 байта. Тип — не то же, что переменная.

---

### 2. Простые типы

- [x] Приведите примеры простых (скалярных) типов.

**Ответ.** Один элемент: целый (`int`, `long`; Python `int`), вещественный (`float`, `double`; Python `float`), символьный (`char`; в Python одноэлементная `str`), логический (`bool`).

---

### 3. Составные типы

- [x] Чем составные типы отличаются от простых?

**Ответ.** Простые — одно значение. Составные (структурированные) — набор: **массив** однотипных элементов, **строка**, **запись/структура** разнотипных полей, **кортеж**. Пример: `double a[10]`, `struct Point { double x, y; }`.

---

### 4. Абстрактный тип данных

- [x] Что такое абстрактный тип данных?

**Ответ.** Тип, заданный **набором операций**, без фиксации представления («чёрный ящик»). Примеры: список, стек, очередь, дерево, множество, **класс**. Важно, *что* можно делать, а не *как* хранится.

---

### 5. Статическая и динамическая типизация

- [x] Чем статическая типизация отличается от динамической?

**Ответ.** **Статическая** (C++, Java): тип объявляется явно и проверяется при компиляции (`int a = 5;`). **Динамическая** (Python): тип определяется при выполнении (`a = 5` → `int`). В Python типы есть, они не «отсутствуют». Сильная типизация не смешивает несовместимые типы без явного преобразования.

---

### 6. Представление в памяти

- [x] Как представляются целые и вещественные числа в памяти?

**Ответ.** Целые — двоичная система, отрицательные в **дополнительном коде** (`int` ≈ 4 байта). Вещественные — IEEE 754:
$$
x=(-1)^s\cdot m\cdot 2^{e}
$$
($s$ — знак, $m$ — мантисса, $e$ — порядок). `float` ~ 7 цифр, `double` ~ 15. Хранение **приближённое**: $0.1+0.2\neq 0.3$.

---

### 7. Константы

- [x] Что такое константа? Как задаются константы в C++ и Python?

**Ответ.** Именованное или литеральное значение, **не изменяемое** при выполнении. Виды: литералы (`42`, `3.14`), именованные, макросы `#define`, перечисления. C++: `const double PI = 3.14159;`, `constexpr`. Python: нет жёсткого `const` — соглашение ЗАГЛАВНЫМИ (`PI = 3.14159`).

---

### 8. Атрибуты переменной

- [x] Назовите атрибуты переменной.

**Ответ.** Переменная — именованная область памяти, значение может меняться. Атрибуты: **имя**, **тип**, **значение**, **адрес**. У константы — значение и тип (изменение запрещено).

---

### 9. Объявление и инициализация

- [x] Чем отличается объявление переменной от инициализации?

**Ответ.** **Объявление** вводит имя и тип: `int a;` (значение ещё не задано). **Инициализация** задаёт начальное значение: `int b = 10;` (объявление + инициализация). В Python создание обычно сразу с значением: `a = 10`.

---

### 10. Область видимости

- [x] Что такое область видимости переменной?

**Ответ.** Часть кода, где имя доступно. **Время жизни** — когда память выделена. Локальные — внутри функции (живут, пока функция работает); глобальные — модуль/программа; статические — сохраняют значение между вызовами. Видимость и время жизни — разные понятия.

---

## 12. Литература

- Страуструп Б. — Язык программирования C++: гл. «Типы и объявления».
- Сузи Р.А. — Язык программирования Python: §§ о типах данных и переменных.
- Вабищевич П.Н. — Численные методы. Вычислительный практикум (Python): практическое использование типов в численных расчётах.
- Полный список: [[Учебно-методическое обеспечение и информационное обеспечение программы вступительного экзамена в аспирантуру по специальности]]

---

## 13. Как должен выглядеть ответ на экзамене

### Образец устного ответа

> [!example] Устно (3–5 минут)
>
> 1. Тип данных = множество значений + допустимые операции + представление в памяти.
> 2. Классификация: простые (int, float, char, bool), составные (массивы, строки, записи), абстрактные (списки, классы).
> 3. Способы задания: статическая типизация (C++, Java) — тип объявляется явно; динамическая (Python) — определяется автоматически.
> 4. Константа — значение не изменяется. В C++: `const double PI = 3.14;`. В Python: `PI = 3.14` (по соглашению).
> 5. Переменная — именованная область памяти: имя + тип + значение + адрес.
> 6. Представление: целые — двоичное дополнение; вещественные — IEEE 754 (мантисса + порядок).
> 7. Пример из численных методов: `N` — константа (число узлов), `h`, `tau` — переменные (шаги сетки).

### Образец письменного ответа

> [!abstract] Письменно (структура для тетради / бланка)
>
> 1. **Тип данных** = домен (значения) + операции + представление.
> 2. **Классификация:**
>     - простые: `int`, `float`, `char`, `bool`;
>     - составные: массивы, строки, структуры;
>     - АТД: списки, классы.
> 3. **Способы задания:**
>     - статическая типизация: `int a = 5;` (C++);
>     - динамическая: `a = 5` (Python).
> 4. **Константа** — неизменяемое значение:
>     - C++: `const double PI = 3.14159;`
>     - Python: `PI = 3.14159` (соглашение).
> 5. **Переменная** — атрибуты: имя, тип, значение, адрес.
> 6. **Представление в памяти:**
>     - целые: двоичное дополнение;
>     - вещественные: IEEE 754, $x = (-1)^s \cdot m \cdot 2^e$.
> 7. **Пример:** `const int N = 100; double h = 1.0/N;` — константа и переменная в разностной схеме.

### Чеклист устно / письменно

| Тема                     | Устно                          | Письменно                  |
| ------------------------ | ------------------------------ | -------------------------- |
| Тип данных               | «значения + операции + память» | определение + 3 компонента |
| Простые типы             | int, float, char, bool         | таблица типов              |
| Составные типы           | массивы, строки                | примеры                    |
| АТД                      | «чёрный ящик»                  | определение + примеры      |
| Статическая/динамическая | C++ vs Python                  | примеры кода               |
| Константа                | «не меняется»                  | `const` / соглашение       |
| Переменная               | имя + тип + значение + адрес   | таблица атрибутов          |
| Представление            | IEEE 754 для вещественных      | формула $(-1)^s m 2^e$     |

---

Предыдущий: [[47-Системы-и-языки-программирования]] · Следующий: [[49-Выражения-операции-операторы]]
